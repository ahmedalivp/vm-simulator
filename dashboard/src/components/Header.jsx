export default function Header() {
    return (
        <header style={{ marginBottom: "30px" }}>
            <h1>Virtual CPU Simulator Dashboard</h1>

            <p>
                Track the implementation progress of the VM project.
            </p>
        </header>
    );
}